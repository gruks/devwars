---
phase: 07-sandbox-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - backend/.env
autonomous: false
user_setup: []

must_haves:
  truths:
    - "No SANDBOX_SERVICE_URL environment variable needed"
    - "Backend starts successfully without sandbox-service running"
    - "Code execution works for all supported languages"
  artifacts:
    - path: "backend/.env"
      not_contains: "SANDBOX_SERVICE_URL"
  key_links:
    - from: "backend startup"
      to: "Docker daemon"
      via: "sandbox.js spawn docker commands"
      pattern: "docker run"
---

<objective>
Remove sandbox-service configuration and verify the integrated sandbox execution works correctly for all supported languages.

Purpose: Complete the single-port deployment by removing unnecessary configuration and validating the integration.
Output: Clean .env file and verified code execution for all languages.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan context
@.planning/phases/07-sandbox-integration/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove SANDBOX_SERVICE_URL from environment</name>
  <files>backend/.env</files>
  <action>
Edit `backend/.env` to remove the SANDBOX_SERVICE_URL configuration line.

Current content includes:
```
# Sandbox Service (Code Execution)
SANDBOX_SERVICE_URL=http://localhost:3001
```

Remove these lines entirely - the sandbox is now integrated directly into the backend.

Do NOT remove any other configuration (PORT, NODE_ENV, ALLOWED_ORIGINS, MONGODB_URI, REDIS_URL, JWT settings, etc.)
  </action>
  <verify>
    - backend/.env does not contain SANDBOX_SERVICE_URL
    - All other environment variables remain intact
  </verify>
  <done>
Environment file cleaned of sandbox-service references. Backend no longer expects external sandbox service.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify sandbox integration works</name>
  <files>N/A</files>
  <action>
User verification of sandbox integration:
1. Start backend without sandbox-service: `cd backend && npm run dev`
2. Verify backend starts on port 5000 without sandbox-service connection errors
3. Test JavaScript and Python execution via API
4. Confirm Docker containers spawn during execution
5. Confirm single-port deployment (no port 3001 needed)
  </action>
  <verify>
Follow the how-to-verify steps below and confirm all tests pass.
  </verify>
  <done>
User confirms all supported languages execute correctly and single-port deployment works.
  </done>
  <what-built>
Integrated sandbox module in backend:
- sandbox.js copied to backend/src/services/
- execution.service.js updated to use local sandbox
- SANDBOX_SERVICE_URL removed from .env
  </what-built>
  <how-to-verify>
1. Start the backend without starting sandbox-service:
   ```bash
   cd backend && npm run dev
   ```

2. Verify backend starts successfully on port 5000 (no errors about sandbox-service connection)

3. Test code execution for each language via API:
   ```bash
   # Test JavaScript
   curl -X POST http://localhost:5000/api/v1/execution/run \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer YOUR_TOKEN" \
     -d '{"code":"console.log(1+1);","language":"javascript","testCases":[{"input":"","expectedOutput":"2"}]}'

   # Test Python  
   curl -X POST http://localhost:5000/api/v1/execution/run \
     -H "Content-Type: application/json" \
     -H "Authorization: Bearer YOUR_TOKEN" \
     -d '{"code":"print(1+1)","language":"python","testCases":[{"input":"","expectedOutput":"2"}]}'
   ```

4. Verify Docker containers are spawned (check `docker ps` during execution)

5. Confirm single-port deployment (only port 5000 needed, no port 3001)
  </how-to-verify>
  <resume-signal>Type "approved" if all languages execute correctly, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- [ ] backend/.env does not contain SANDBOX_SERVICE_URL
- [ ] Backend starts without sandbox-service running
- [ ] JavaScript execution works
- [ ] Python execution works (via Docker)
- [ ] No HTTP calls to sandbox-service
- [ ] Only port 5000 required
</verification>

<success_criteria>
- Environment configuration cleaned
- Backend operates independently without sandbox-service
- Code execution verified for at least JavaScript and Python
- Single-port deployment confirmed (port 5000 only)
</success_criteria>

<output>
After completion, create `.planning/phases/07-sandbox-integration/07-02-SUMMARY.md`
</output>
