---
phase: 08-test-execution-enhancement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - code-arena/src/components/room/TestCasePanel.tsx
  - backend/src/modules/questions/question.model.js
  - backend/src/modules/questions/question.seed.js
autonomous: true
user_setup: []

must_haves:
  truths:
    - "TestCasePanel supports adding custom test cases dynamically"
    - "Backend question model stores custom test cases with proper validation"
    - "Custom test cases are persisted to database and available for evaluation"
    - "Users can add, edit, and delete custom test cases"
  artifacts:
    - path: "code-arena/src/components/room/TestCasePanel.tsx"
      provides: "Dynamic custom test case management interface"
      contains: "Add/edit/delete test case functionality"
    - path: "backend/src/modules/questions/question.model.js"
      provides: "Updated question model with custom test case support"
      contains: "Custom test cases array with validation"
    - path: "backend/src/modules/questions/question.seed.js"
      provides: "Updated seed data with custom test case examples"
      contains: "Custom test case format and examples"
  key_links:
    - from: "code-arena/src/components/room/TestCasePanel.tsx"
      to: "backend/src/modules/questions/question.model.js"
      via: "API calls for test case management"
      pattern: "POST.*test-cases.*PUT.*test-cases.*DELETE.*test-cases"
---

<objective>
Add custom test case functionality to allow users to create, edit, and manage their own test cases for better practice and debugging.

Purpose: Enable users to extend existing test cases with custom scenarios and edge cases for comprehensive testing.
Output: Dynamic test case management system with backend persistence.
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Source files to reference
@code-arena/src/components/room/TestCasePanel.tsx
@backend/src/modules/questions/question.model.js
@backend/src/modules/questions/question.seed.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance TestCasePanel with custom test case management</name>
  <files>code-arena/src/components/room/TestCasePanel.tsx</files>
  <action>
Enhance TestCasePanel to support custom test case management:

1. **Add custom test case interface:**
   - Add "Add Custom Test Case" button in the Testcase tab
   - Create modal/form for adding new test cases
   - Form fields: Input parameters, Expected output, Description, Is Hidden (checkbox)
   - Add validation for required fields and input format

2. **Add test case editing functionality:**
   - Add edit button for each test case
   - Create inline edit mode or modal for editing
   - Allow editing of input, output, description, and hidden status
   - Add cancel/save functionality for edits

3. **Add test case deletion functionality:**
   - Add delete button for each test case (except seeded ones)
   - Add confirmation dialog for deletion
   - Allow users to delete their custom test cases

4. **Add test case organization:**
   - Separate seeded test cases from custom test cases visually
   - Allow reordering of custom test cases
   - Add drag-and-drop functionality for reordering
   - Show custom test case count separately

5. **Add test case filtering and search:**
   - Add search functionality to find specific test cases
   - Filter by passed/failed status
   - Filter by custom/seeded test cases
   - Add clear filters button

6. **Add test case statistics:**
   - Show total test cases count
   - Show custom test cases count
   - Show pass rate for custom test cases
   - Display test case creation/modification dates

7. **Improve visual design:**
   - Use card-based layout for test cases
   - Add color coding for different test case types
   - Use better typography for test case names and descriptions
   - Add hover effects and transitions

8. **Add keyboard shortcuts:**
   - Ctrl+N for adding new test case
   - Ctrl+E for editing selected test case
   - Ctrl+D for deleting selected test case
   - Ctrl+F for focusing search

9. **Add accessibility features:**
   - Proper ARIA labels for all interactive elements
   - Keyboard navigation support
   - Screen reader support for test case information
   - High contrast mode support

10. **Add test case sharing:**
    - Allow users to copy test case as JSON
    - Allow users to import test cases from JSON
    - Add share button for test cases
    - Generate shareable links for test cases

Maintain existing functionality while adding these new features.
  </action>
  <verify>
    - Add Custom Test Case button is visible and functional
    - Modal/form for adding test cases works correctly
    - Edit functionality allows modifying test cases
    - Delete functionality removes custom test cases
    - Test cases are organized and filterable
    - Statistics and analytics are accurate
    - Visual design is improved and consistent
    - Keyboard shortcuts work as expected
    - Accessibility features are implemented
    - Test case sharing functionality works
  </verify>
  <done>
TestCasePanel supports dynamic custom test case management with add/edit/delete functionality.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update backend to support custom test cases</name>
  <files>
    backend/src/modules/questions/question.model.js
    backend/src/modules/questions/question.seed.js
  </files>
  <action>
Update backend to support custom test cases:

1. **Update question model for custom test cases:**
   - Add `customTestcases` array field to question schema
   - Add `userId` field to track custom test case owner
   - Add `createdAt` and `updatedAt` timestamps for custom test cases
   - Add validation for custom test case format
   - Add indexes for efficient querying of custom test cases

2. **Add custom test case API endpoints:**
   - POST /api/v1/questions/:questionId/test-cases - Add custom test case
   - PUT /api/v1/questions/:questionId/test-cases/:testcaseId - Update custom test case
   - DELETE /api/v1/questions/:questionId/test-cases/:testcaseId - Delete custom test case
   - GET /api/v1/questions/:questionId/test-cases - Get all test cases (seeded + custom)
   - POST /api/v1/test-cases/validate - Validate test case format

3. **Add custom test case controller methods:**
   - `addCustomTestcase` - Add new custom test case with validation
   - `updateCustomTestcase` - Update existing custom test case
   - `deleteCustomTestcase` - Delete custom test case
   - `getCustomTestcases` - Get all test cases for a question
   - `validateTestcase` - Validate test case format and content

4. **Add custom test case validation:**
   - Validate input format (named parameters or simple format)
   - Validate output format (JSON-compatible strings)
   - Validate test case description length
   - Validate hidden status is boolean
   - Validate user permissions for custom test cases

5. **Update question seed data:**
   - Add example custom test cases to seed data
   - Add custom test case examples for each question
   - Add test case sharing examples
   - Add edge case examples for testing

6. **Add custom test case utilities:**
   - `formatTestcaseForDisplay` - Format test case for UI display
   - `parseTestcaseInput` - Parse test case input format
   - `validateTestcaseFormat` - Validate test case format
   - `generateTestcaseId` - Generate unique test case IDs

7. **Add custom test case middleware:**
   - `authenticateCustomTestcase` - Authenticate custom test case operations
   - `authorizeCustomTestcase` - Authorize custom test case access
   - `validateCustomTestcase` - Validate custom test case data
   - `sanitizeCustomTestcase` - Sanitize custom test case input

8. **Add custom test case error handling:**
   - Custom error classes for test case operations
   - Error messages for validation failures
   - Error handling for database operations
   - Error logging for debugging

9. **Add custom test case documentation:**
   - API documentation for custom test case endpoints
   - Examples of custom test case usage
   - Error response examples
   - Best practices for custom test cases

10. **Add custom test case testing:**
    - Unit tests for custom test case functionality
    - Integration tests for API endpoints
    - End-to-end tests for custom test case workflow
    - Performance tests for custom test case operations

Ensure all custom test case functionality is secure, performant, and well-documented.
  </action>
  <verify>
    - Custom test cases are stored in database with proper schema
    - API endpoints for custom test case management work correctly
    - Custom test case validation prevents invalid data
    - User permissions are properly enforced
    - Seed data includes custom test case examples
    - Error handling is comprehensive and user-friendly
    - Documentation is complete and accurate
    - Tests cover all custom test case functionality
  </verify>
  <done>
Backend supports custom test cases with proper storage, validation, and API endpoints.
  </done>
</task>

</tasks>

<verification>
- [ ] TestCasePanel supports adding, editing, and deleting custom test cases
- [ ] Backend question model stores custom test cases with proper validation
- [ ] API endpoints for custom test case management work correctly
- [ ] Custom test cases are persisted and available for evaluation
- [ ] User permissions are properly enforced
- [ ] Visual design is improved for better user experience
- [ ] All functionality is secure and well-documented
</verification>

<success_criteria>
- Users can create, edit, and delete custom test cases
- Custom test cases are stored in database with proper validation
- Custom test cases are available for evaluation alongside seeded cases
- User permissions are properly enforced
- Visual design is improved for better usability
- All functionality is secure and well-documented
</success_criteria>

<output>
After completion, create `.planning/phases/08-test-execution-enhancement/08-03-SUMMARY.md`
</output>